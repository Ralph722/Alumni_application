rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Very permissive rules - allow all authenticated users to read/write
    // This is for development/testing purposes
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}

